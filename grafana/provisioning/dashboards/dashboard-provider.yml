apiVersion: 1

providers:
  - name: DMARC分析ダッシュボード
    orgId: 1
    folder: DMARC分析
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
